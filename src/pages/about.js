import React from "react"
import Header from "../components/header"

export default function About() {
  return (
    <div style={{ color: `teal` }}>
      <Header headerText="This is property in Header Component"/>
      <p>Such wow. Very React.</p>
    </div>
  )
}